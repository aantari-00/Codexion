This folder contains a small smoke-test script for the simulation.
It is not part of the graded submission (see the subject: test
programs are encouraged but not required), it just makes it quick to
check that nothing broke after a change.

Run it from the repository root:

```bash
make
./test/run_tests.sh
```
